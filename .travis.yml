language: php

php:
  - 7
  - 5.6
  - 5.5
  - hhvm

before_script:
  - composer self-update && composer install --dev

script:
  - ./vendor/bin/phpunit

after_success:
  - bash <(curl -s https://codecov.io/bash)
